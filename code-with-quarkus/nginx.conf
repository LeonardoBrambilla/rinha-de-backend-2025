events {
    worker_connections 1024;
}

http {
    upstream backend {
        server app-1:8080;
        server app-2:8080;
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://backend;
        }
    }
}
